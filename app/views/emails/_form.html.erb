<%= form_for(@email) do |f| %>
  <% if @email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
      <% @email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :recipients %><br>
      <%= f.text_area :recipients ,size: "30x5"%>
    </div>

    <div class="field">
      <%= f.label :call ,"Call to Phone" %><br>
      <%= f.text_area :call ,size: "60x3"%>
    </div>
    <div class="field">
      <%= f.label :voice ,"Voice message" %><br>
      <%= f.text_area :voice,size: "60x3" %>
    </div>
    <div class="field">
      <%= f.label :audio ,"Audio message" %><br>
      <%= f.text_area :audio,size: "60x10" %>
    </div>



    <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>
  </div>


  <div class="field">
    <%= f.label :messag %><br>
    <%= f.text_area :message,size: "30x5" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
